# IDSAI Corp. API

Платформа IDSAI

## Auth env

Для безопасного запуска auth-контура теперь обязательно задать:

- `JWT_SECRET` — не короче 32 символов
- `PUBLIC_BASE_URL` — публичный адрес приложения, используется в письмах подтверждения email и сброса пароля
